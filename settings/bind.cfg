// Remove all existing binds.
unbindall

// Updates game keyboard layout to the current Windows keyboard setting.
key_updatelayout

// Slot binds.
bind "1"             "slot1; keycontainer"                                                                    // Slot 1
bind "2"             "slot2; keycontainer"                                                                    // Slot 2
bind "3"             "slot3; keycontainer"                                                                    // Slot 3
bind "4"             "slot4; +lookatweapon; -lookatweapon; keycontainer"                                      // Slot 4
bind "5"             "slot5; +lookatweapon; -lookatweapon; keycontainer"                                      // Slot 5
bind "6"             "slot9; +lookatweapon; -lookatweapon; keycontainer"                                      // Decoy Grenade.
bind "7"             "slot11; +lookatweapon; -lookatweapon; keycontainer"                                     // Zeus
bind "8"             "slot12; keycontainer"                                                                   // Medi-Shot
bind "9"             ""                                                                                       // -
bind "0"             "callvote starttimeout"                                                                  // Start timeout

// Movement binds.
bind "w"             "+forward; keycontainer"                                                                 // Move forward.
bind "a"             "+left; keycontainer"                                                                    // Strafe left.
bind "s"             "+back; keycontainer"                                                                    // Move back.
bind "d"             "+right; keycontainer"                                                                   // Strafe right.


bind "mouse_x"       "mousecontainer; yaw"                                                                    // Controls the mouse screen movement on vertical axis.
bind "mouse_y"       "mousecontainer; pitch"                                                                  // Controls the mouse screen movement on horizontal axis.

bind "x_axis"        "rightleft"                                                                              // Controls the screen movement on axis X.
bind "y_axis"        "!forwardback"                                                                           // Controls the screen movement on axis Y.
bind "u_axis"        "yaw"                                                                                    // Controls the screen movement on vertical axis.
bind "r_axis"        "pitch"                                                                                  // Controls the screen movement on horizontal axis.


// Chat binds.
bind "u"             "messagemode2"                                                                           // Team messages (chat only with the team).
bind "y"             "messagemode"                                                                            // Broadcast and Team messages (chat with all).

// Other letter keys.
bind "c"             "switchhands;keycontainer"                                                               // Switch hands
bind "b"             "buymenu; keycontainer"                                                                  // Open the buy menu.
bind "e"             "+betteruse; keycontainer"                                                               // Execute actions
bind "f"             "+lookatweapon; keycontainer"                                                            // Inspect the weapon.
bind "g"             "drop; keycontainer"                                                                     // Drop the current weapon.
bind "h"             "kaoprint; keycontainer"                                                                 // Randomized kaomoji
bind "i"             "handswap; keycontainer"                                                                 // Funny doom hehe
bind "j"             ""                                                                                       // -
bind "k"             "callvote; keycontainer"                                                                 // Open the vote menu.
bind "l"             "say real; keycontainer"                                                                 // literally just says real because i'm brain damaged
bind "m"             "teammenu; keycontainer"                                                                 // Open the team selection menu.
bind "n"             ""                                                                                       // -
bind "o"             "shootr_toggle; keycontainer"                                                            // Toggle u/Dentstring's better follow recoil bind
bind "p"             "toggle spec_show_xray; keycontainer"                                                    // Toggles x-ray when spectating.
bind "q"             "togglehud; keycontainer"                                                                // Switch to knife while holding, then switch back after letting go.
bind "r"             "+reload; keycontainer"                                                                  // Reload the currently weapon.
bind "t"             "+spray_menu; keycontainer"                                                              // Open the spray menu to apply spray on the aimed surface.
bind "v"             "nc; keycontainer"                                                                       // Noclip
bind "x"             "+attack;slot5"                                                                          // Fake plant
bind "z"             "playerchatwheel CW.Cheer galls"                                                        // -

// Special keys.
bind "escape"        "cancelselect; keycontainer"                                                             // Open / close the game menu.
bind "tab"           "+scorenet; +showscores; keycontainer"                                                   // Shows the scoreboard with additional info.
bind "capslock"      ""                                                                                       // -
bind "shift"         "+sprint; keycontainer"                                                                  // Walk.
bind "rshift"        ""                                                                                       // -
bind "ctrl"          "+duck; keycontainer"                                                                    // Duck / Crouch.
bind "rctrl"         ""                                                                                       // -
bind "alt"           "+secondbinds; keycontainer"                                                             // Toggle the secondary set of keys <hold to activate>
bind "ralt"          ""                                                                                       // -
bind "space"         "+jb; keycontainer"                                                                      // Jump bug bind.
bind "enter"         ""                                                                                       // -
bind "backspace"     ""                                                                                       // -
bind "ins"           ""                                                                                       // -
bind "del"           ""                                                                                       // -
bind "home"          ""                                                                                       // -
bind "end"           ""                                                                                       // -
bind "pgup"          "exec settings/audio.cfg; keycontainer"                                                  // Execute default audio settings (revert the sound).
bind "pgdn"          "volume 0; keycontainer"                                                                 // Mute the sound.
bind "scrolllock"    ""                                                                                       // -

// Punctuation keys.
bind "`"             "toggleconsole"                                                                          // Open the developer console.
bind ","             "autoexec"                                                                               // Execute the autoexec.cfg.
bind "."             "print; keycontainer"                                                                    // Faggot / tranny binds
bind "/"             ""                                                                                       // -
bind "semicolon"     "say nuh uh; keycontainer"                                                               // same reason as the bind on L, i'm just brain damaged
bind "'"             ""                                                                                       // -
bind "\"             ""                                                                                       // -
bind "["             ""                                                                                       // -
bind "]"             ""                                                                                       // -
bind "-"             ""                                                                                       // -
bind "="             ""                                                                                       // -

// Cursor control keys.
bind "uparrow"       "+forward; keycontainer"                                                                 // Move forward.
bind "downarrow"     "+back; keycontainer"                                                                    // Move back.
bind "leftarrow"     "+turnleft; keycontainer"                                                                // Rotate to the left
bind "rightarrow"    "+turnright; keycontainer"                                                               // Rotate to the right

// Mouse keys.
bind "mwheelup"      "j; keycontainer"                                                                        // Desubticked Jump
bind "mwheeldown"    "j; keycontainer"                                                                        // Desubticked Jump
bind "mouse1"        "+shootr; keycontainer"                                                                  // -
bind "mouse2"        "+attack2; keycontainer"                                                                 // -
bind "mouse3"        "player_ping;say Cannot ping. Ping is on cooldown."                                      // Ping
bind "mouse4"        "+lj; keycontainer"                                                                      // Long jump
bind "mouse5"        "+voicerecord"                                                                           // Voice Comms

// Function keys.
bind "f1"            ""                                                                                       // -
bind "f2"            ""                                                                                       // -
bind "f3"            "togglehud; keycontainer"                                                                // Toggles hud and only draws death notices
bind "f4"            "afk-move; keycontainer"                                                                 // AFK script toggle
bind "f5"            "togglethirdperson; keycontainer"                                                        // hehe funny minecraft
bind "f6"            "clutch_mode_toggle; select; keycontainer"                                               // Toggle clutch mode (silence the teammates until next round or death)
bind "f7"            ""                                                                                       // -
bind "f8"            "ignore-radio; keycontainer"                                                             // Toggle radio messages
bind "f9"            "fixer; keycontainer"                                                                    // Execute a script which can fix graphical glitches, sound glitches, hud bugs etc.
bind "f10"           ""                                                                                       // -
bind "f11"           ""                                                                                       // -
bind "f12"           ""                                                                                       // -

// Numeric keypad keys.
bind "numlock"        ""                                                                                      // -
bind "kp_divide"      "exec CS2AutoPovCommand"                                                                // -
bind "kp_multiply"    "exec selfbot"                                                                          // -
bind "kp_minus"       ""                                                                                      // -
bind "kp_plus"        ""                                                                                      // -
bind "kp_enter"       "exec a"                                                                                // -
bind "kp_del"         ""                                                                                      // -
bind "kp_0"           ""                                                                                      // -
bind "kp_1"           "altchat1"                                                                              // -
bind "kp_2"           "altchat2"                                                                              // -
bind "kp_3"           "say can you shur.up man??? uuggghh~ *shakes butt*"                                     // -
bind "kp_4"           "say guys you're getting your asses stretched by a girl right now cmon"                 // -
bind "kp_5"           "say bind m say meow"                                                                   // -
bind "kp_6"           "altchat6"                                                                              // -
bind "kp_7"           "say im gonna throw a rock through your window"                                         // -
bind "kp_8"           "say sorry my aim is bad my tits hurt so bad"                                           // -
bind "kp_9"           "altchat9"                                                                              // -

// Prevent buy wheel from purchasing via number keys.
// Default: 0
cl_buywheel_nonumberpurchasing "1"

// Zoom button hold.
// Default: 1
//
// >> Holding the zoom button should heave as if it was pressed repetedly.
//
// <> Values:
// 0: Repetitive zoom.
// 1: One click.
cl_debounce_zoom "0"

// Detach silencer on supported weapons.
// Default: 0
//
// >> Removal of silencers on the M4A1-S and USP-S.
//
// <> Values:
// 0: Cannot detach.
// 1: Press secondary fire to detach.
cl_silencer_mode "1"

// Pressing the +use key will open the buy menu if in a buy zone (just as if the 'buy' key is pressed).
// Default: 1
cl_use_opens_buy_menu "0"

// Name of the binding to enable mouse selection in the scoreboard.
// Default: +attack2
cl_scoreboard_mouse_enable_binding "+attack2"

// Set the key to use for donation in the buy menu.
// Default: 0
//
// <> Values:
// 0: Left Control.
// 1: Left Alt.
// 2: Left Shift.
cl_buywheel_donate_key "0"

// Duck toggle.
// Default: 0
//
// <> Values:
// 0: Hold to duck.
// 1: Press to toggle duck.
option_duck_method "0"

// Walk toggle.
// Default: 0
//
// <> Values:
// 0: Hold to walk.
// 1: Press to toggle walk.
option_speed_method "0"

// When tapping the radial radio button, leave a ping if nothing is selected within the time in seconds set in 'cl_radial_menu_tap_duration'.
// Default: 1
cl_radial_radio_tap_to_ping "1"

// Joystick controller support.
// Default: 0
joystick "0"


// C01 #ffffff  - Normal, white
// C02 #ff0000  - "Use old colors", dark red
// C03 #ba81f0  - Player name, team color (CT - #a2c6ff, T - #ffdf93)
// C04 #40ff40  - Location, bright green
// C05 #bfff90  - Achievement, light green
// C06 #a2ff47  - Award, bright green
// C07 #ff4040  - Penalty, light red
// C08 #c5cad0  - Silver
// C09 #ede47a (tab character) - Gold

// C10 #b0c3d9 (line feed) - Common?
// C11 #5e98d9  - Uncommon, very light blue
// C12 #4b69ff  - Rare, darker blue
// C13 #8847ff (carriage return) - Mythical?
// C14 #d32ce6  - Legendary, purple
// C15 #eb4b4b  - Ancient, lighter red
// C16 #e4ae39  - Immortal, orange